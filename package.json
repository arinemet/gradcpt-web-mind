{
  "name": "gradcpt-web-mind",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "deploy": "bun run build && gh-pages -d dist -r git@github.com:arinemet/gradcpt-web-mind.git",
    "deploy:pavlovia": "bash scripts/deploy-pavlovia.sh"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "typescript": "~6.0.2",
    "vite": "^8.0.12"
  },
  "dependencies": {
    "@jspsych/plugin-survey-multi-choice": "^1.1.3",
    "jspsych": "^7.3.4"
  }
}
